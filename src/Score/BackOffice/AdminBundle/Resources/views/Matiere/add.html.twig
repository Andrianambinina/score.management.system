{% extends "backoffice/base_admin.html.twig" %}

{% block title %}{{ parent() }} Ajout matière{% endblock %}

{% set menu_matiere = true %}

{% block body %}
    <section class="content">
        <div class="row">
            <div class="col-xs-8">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Ajout matière</h3>
                    </div>
                    {{ form_start(form) }}
                    <div class="box-body">
                        {{ form_row(form.libelle) }}
                        {{ form_row(form.coefficient) }}

                        <div class="box-footer">
                            <input type="submit" class="btn btn-primary" name="new_matiere" value="Envoyer"/>
                            <a href="{{ path('matiere_index') }}" class="btn btn-default">
                                Retour à la liste
                            </a>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
